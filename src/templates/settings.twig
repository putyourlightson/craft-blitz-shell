{% import "_includes/forms" as forms %}


{{ forms.editableTableField({
    id: 'commands',
    name: 'commands',
    label: "Commands"|t('blitz'),
    instructions: "Shell commands to be executed."|t('blitz'),
    cols: [{
        type: 'singleline',
        heading: "Command"|t('blitz'),
        code: true,
    }],
    rows: deployer.commands,
    addRowLabel: "Add a command"|t('blitz'),
    allowAdd: true,
    allowDelete: true,
    allowReorder: true,
}) }}
